/**
 * ImportError - represents a single CSV row that failed validation.
 *
 * Columns (see database/schema.sql):
 *  id, import_id, row_number, reason, raw_data, created_at
 */

module.exports = {};

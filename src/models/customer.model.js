/**
 * Customer - represents a single valid row imported from a CSV file.
 *
 * Columns (see database/schema.sql):
 *  id, import_id, name, email, phone, created_at
 *
 * NOTE: The assessment PDF does not specify the CSV column schema.
 * For this implementation we assumed: name, email, phone
 * (documented in README.md under "Assumptions").
 */

module.exports = {};
